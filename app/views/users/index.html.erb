<h1>All Users</h1>

<ul id="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.username, edit_user_path(user) %>
    </li>
  <% end %>
</ul><br>

<%= link_to 'New user', new_user_path, :class => "new" %>
